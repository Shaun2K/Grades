<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.overallgpa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Summer2"></activity>
        <activity android:name=".Summer1" />
        <activity android:name=".Winter2" />
        <activity android:name=".Winter1" />
        <activity android:name=".Spring2" />
        <activity android:name=".Spring1" />
        <activity android:name=".Fall2" />
        <activity android:name=".Fall1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>